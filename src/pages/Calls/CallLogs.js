import React from 'react';
import { Box, Typography, Paper } from '@mui/material';

const CallLogs = () => (
  <Box>
    <Typography variant="h4" gutterBottom>
      Call Logs
    </Typography>
    <Paper sx={{ p: 3 }}>
      <Typography variant="body1">
        Call logs interface will be implemented here.
      </Typography>
    </Paper>
  </Box>
);

export default CallLogs;
